import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.thanayut.learnhub',
  appName: 'LearnHub',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
